{
  "name": "REPLACE-ME-mcp",
  "version": "0.1.0",
  "description": "MCP server for REPLACE-ME (one-line description).",
  "type": "module",
  "private": true,
  "bin": {
    "REPLACE-ME-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.4.5",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
